.page-levels-edit{:data => {:level_id => @level.id.to_s}}
  = simple_form_for @level do |f|
    = f.input :number

    = f.input :name

    = f.association :posts, label_method: :name, as: :check_boxes

    = f.button :submit, "提交"
